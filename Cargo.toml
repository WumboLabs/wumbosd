[package]
name = "wumbosd"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.48", features = ["macros", "rt", "signal"] }
zbus = { version = "5.14", default-features = false, features = ["tokio"] }
